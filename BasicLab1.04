#include <stdio.h>

int main(void)
{
    int N, sum = 0, i = 0;

    if (scanf("%d", &N) != 1)
        return 1;

    while (i < N)
    {
        sum += i;
        i++;
    }

    printf("%d\n", sum);
    return 0;
}

