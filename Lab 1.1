#include <stdio.h>
int main() {
    int G;
    if (scanf("%d", &G) != 1) {
        return 1;
    }
    if (G > 0) {
        int count = 0;
        while (count < G) {
            puts("Hello my D");
            count++;
        }
    } else {
        printf("Error\n");
    }
    return 0;
}
