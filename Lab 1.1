#include <stdio.h>

int main() {
    int D;

    if (scanf("%d", &D) != 1)
        return 1;

    if (D > 0) {
        int count = 0;
        while (count < D) {
            puts("Hello DIW");
            count++;
        }
    } else {
        printf("Error");
    }

    return 0;
}
