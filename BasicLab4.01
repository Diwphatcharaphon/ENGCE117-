#include <stdio.h>

int main() {
    int n;
    
    if (scanf("%d", &n) != 1) {
        return 1;
    }

    int data[n];

    for (int idx = 0; idx < n; idx++) {
        if (scanf("%d", &data[idx]) != 1) {
            return 1;
        }
    }

    for (int idx = n - 1; idx >= 0; idx--) {
        printf("%d\n", data[idx]);
    }

    return 0;
}

